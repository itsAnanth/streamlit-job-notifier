[project]
name = "job-notifier"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "streamlit",
    "pandas",
    "scikit-learn",
    "joblib",
    "beautifulsoup4",
    "requests",
    "lxml",
    "schedule>=1.2.2",
]
